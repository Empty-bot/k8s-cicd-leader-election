apiVersion: v1
kind: Service
metadata:
  name: orchestrator
spec:
  selector:
    app: orchestrator
  type: NodePort
  ports:
    - name: webhook
      port: 8081
      targetPort: 8081
      nodePort: 30080
    - name: healthz
      port: 8080
      targetPort: 8080
      nodePort: 30081
